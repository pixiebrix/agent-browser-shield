# Used by the irrelevant-sections-hide rule (background worker → OpenAI).
# Injected into the built bundle via build.ts define. The unbuilt source has no
# secret; rebuild whenever you rotate the key.
#
# The build also checks the project-root .env (../.env) and falls back to
# MODEL_API_KEY (the convention shared with the Python benchmark scripts).
OPENAI_API_KEY=sk-...
